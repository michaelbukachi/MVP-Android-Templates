package ${packageName};

class ${className}Model {
    private ${className}Contract contract;

    ${className}Model(${className}Contract contract) {
        this.contract = contract;
    }

    void clear() {
        // clean up code
    }
}

